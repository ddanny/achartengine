<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="org.achartengine.demo" android:versionCode="1" android:versionName="1.0">
    <application android:icon="@drawable/chartdroid_icon" android:label="@string/app_name">

        <!-- The following are DEMO providers that you should place in your own apps. -->
      	<provider android:label="Some random data"
   			android:name="DataContentProvider"
	        android:authorities="org.achartengine.demo.provider.test"
		/>


        <activity android:name="Demo"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
    <uses-sdk android:minSdkVersion="4"/>
</manifest> 